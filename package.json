{
  "name": "geo-code-generator",
  "version": "1.0.0",
  "description": "Generate geo codes for countries by region/sub-region selection",
  "scripts": {
    "build": "cp -r . dist/ || xcopy . dist\\ /E /I",
    "dev": "npx serve ."
  },
  "devDependencies": {
    "serve": "^14.2.0"
  }
}